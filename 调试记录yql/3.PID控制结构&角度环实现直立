
1.变量重复定义，
发现与底层驱动里的重复。
修改后，解决

2，
按键led指示启停，逻辑的健壮性与合理性错误
：
我的非健壮性写法，把标志位，置位1，-1
根据按键按下，实现相反数变换。
但是，变量初始值，为0.
所以，不能直接进去我的逻辑判断。
所以，只有手动将平衡车放倒，实现标志位设置为-1，才能正常进行后面判断。
而且，每一次初始化，标志位都会从0开始。

改变逻辑写法：
定义，0，1.！实现反转


3，
添加新的文件，发现，没有找到。
原来是，查找范围，没有改变。
还是文件1的，keil的设置会跟随移植过来的启动程序对应的设置。
需要手动修改

4.
平均pwm的输出最大，最小值，与左右轮的pwm范围一样。
由自己规定，小于电机最大转速即可。我规定pwm:100
但是，电机最大转速需要计算。
不知道！







5.
不能输出角度，
发现：代码错误。
没有实现count直增，不能达到10ms的周期。不能进入逻辑判断，

6.
up在调试时，遇到了无论怎么调节pid参数，及上层程序，都不能让平衡车直立的情况，
原因是底层的配置。
mpu6050的config寄存器，的值决定了，输出值的噪声大小，与输出速度。
显然，噪声越大，速度越快。
当我们，配置值为0x06，这时候，噪声最小，低通滤波器拉满，但是延迟达到了19m，远远大于pid调控周期10Ms,
我们，宁愿有噪声，也要把延迟降到最低。
config的值配置为0x00。
延迟为0ms .
config，MPU6050_SMPLRT_DIV 寄存器的值，决定mpu6050的采样数据刷新频率。
config寄存器值为0x00,对应输出速度，为8khz.
此时，采样速度频率=8/(1+SMPLRT_DIV)
若，SMPLRT_DIV=0x07,则采样速度频率为8/(1+7)=1khz,（1s刷新次数）对应时间为1ms

虽然，小于pid调控周期，mpu6050的性能有点浪费，但是，后续可能会减小pid调控周期，所以，先这样设置。

总结：硬件配置必须给力，不然主程序，pid参数怎么写都不行。

解决及反馈：调整，寄存器值后，加速度的角度波形噪声更大了，（因为没有所以低通滤波器）
但是互补滤波后得到的角度依然平缓。



7.
代码逻辑：if分支

 if(rpwm>100)  rpwm=100;
 else if(rpwm<-100)  rpwm=-100;
 if(lpwm>100)  lpwm=100;
 else if(lpwm<-100)  lpwm=-100;

 if(rpwm>100)  rpwm=100;
if(rpwm<-100)  rpwm=-100;
 if(lpwm>100)  lpwm=100;
 if(lpwm<-100)  lpwm=-100;

 虽然这里的功能一样，但是含义不同。
 第一种，第一个else if作为第一个if的互斥判断。
 第二种写法，每一个if判断，都会检验，占用资源略大。


8.

程序有问题。用老师的out会变化。
我的不会。
硬件驱动没问题，主程序试试。
没有源码，我就用克劳德检查，再自己检查。
现在，先用源码吧



9，

（然后，我的电池不能供电。
只用stlink供电，不好）


10，
滑杆来远程调节目标角度，差分pwm.
查看角度环pid的控制效果
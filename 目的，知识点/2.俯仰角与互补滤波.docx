1.
mpu6050能够测量3轴加速度与角加速度。


平衡车，姿态上只有前后俯仰，所以，我们只计算俯仰角，

a:加速度才测量角度
若，将mpu6050的y轴与平衡车平行放置，
当平衡车倾斜时，重力在x,z轴分量，满足
tan(θ)=ax/az
则俯仰角，可求。    

运行之前的mpu6050测试代码，
显示的是mpu6050的寄存器值。
查看数据手册，将其转换为加速度的单位。

查看寄存器的描述。
打开，ACCEL_CONFIG寄存器的描述。
  AFS_SEL[1:0]的两位的值，决定寄存器对应的加速度量程范围，

当，ACCEL_CONFIG10进制数为，3 ，16进制为0x18,2进制为00011000时，
对应量程范围是，0-16g

由于，寄存器的值是16为数，范围是0-32768
所以，寄存器值/32768*16g，就是加速度

为了方便展示，角度变化，可以使用蓝牙串口，将数据发送给串口助手，切换成图像查看

加速度测量角度的局限性：
物体，加速运行，与姿态发生改变，都会产生x,z轴加速度的变化，所以，用这个方法测量角度，
在出现了加速运动时，加速度会产生干扰。
而且，当物体颠簸运动这时候测量测量到底加速度变化很快，计算得到的角度也会偏离实际角度



b:
陀螺仪测量角度

小车前后倾斜时，陀螺仪也前后倾斜旋转，角速度越快，弹簧受力越大
因此，角速度可以测量。由于，绕y轴旋转，我定义角速度，为gy。
mpu6050函数返回的就是角速度对应的寄存器值


将mpu6050测量到的寄存器值转换为，角速度。
查看数据手册，查看GYRO_CONFIG寄存器描述
FS_SEL[1:0]的值决定角速度量程。

FS_SEL[1:0]为3，即0001100，（寄存器2进制写入）即0x18（函数写入参数为16进制）时，
对应角速度范围，0-2000度/s

寄存器值/32768*2000,即角速度

角速度积分，得到角度变化值。
积分时间为中断时间，如1ms。

角度=上一次中断前角度+角度变化值。


优点；不会受到运动加速度，及颠簸的影响
缺点：1，陀螺仪存在零漂，造成其得到的角度一直发生轻微增加或减少。,
2程序初始化，初始角度为0，不一定符合平衡车的实际姿态。
所以，后续测量的角度变化值准确，角度值不对。

c:互补滤波算法
a,b两种方法各有优缺点，我们设计一种算法将两个方法的优缺点相互抵消，让结果尽量准确。

采用加权平均，得到滤波角度，angle=a*angleacc+(1-a)*anglegyro
为了抑制，陀螺仪不断漂移的现象，将陀螺仪角度算法改变，anglegyro=angle+角度变化值


a可以增大加速度角度的作用。放大他的优缺点。
gy-=10,实现了零漂的增加或减小。
若零漂，gy=10，则零漂基本被解决

调这两个参数的过程，就类似于调pid。本质都是，实验测试，看效果。不断迭代。



1，

在中断里，进行分频。
定时服务函数里，
第一步获取encoder的编码值，进行信号量的转换，得到平均速度
第二步，
处于按钮按下，的开启状态，进行速度环pid的配置

获取输入的实际值，
pid接口返回out输出值，
将out作为内环角度环的输入
每次按下启动按钮，将速度环的参数初始化，

然后，调pid参数


细节解释：
分频周期，大于内环，我这里定为50ms

编码器得到的寄存器值，进行转换得到电机转速
转换，注意小数不要丢失掉。
/44.0
再将左右电机转速，计算得到平均转速，差分速度。
为什么要定义差分速度？
因为这个作为转向环的输入值，这个环用不上
作为速度环的实际值，作为输入值。
目标值，有用户手动给定，默认为0


pid接口不会返回角度，内部将全局变量out更新。

kp,ki,kd，是输入到输出的映射系数，而且是有单位的。

把结构体的out直接给角度环的目标值即可。
对于环的输出值给谁，应该看作用效果是否能够闭环。

速度环的输出，作用与角度环的目标值，能够影响速度环的输入：平均速度实际值。
角度环的输出，作用与平均pwm，能够影响角度环的输入，角度实际值。

每次按下启动按钮，将速度环的参数初始化，
把上一次的速度误差清除


然后，调pid参数
用蓝牙滑杆调参。

调参前，先验证速度环的out极性是否正确。
速度环的作用效果是，通过间接改变角度，来使速度的响应速度更快

把角度环，速度环的p,i,d参数清零。速度环kp给1，
看一下是否能够抑制倾斜。

up是这样做的，但是，我有疑问，角度环的参数为0，怎么能够输出out，即平均pwm呢？
？？？？


然后，由于存在稳态误差，（这个不是很理解，不过这里会用就行）所以需要加上i项。
我这里，先加上左摇杆纵向值远程控制的逻辑，调节速度来控制并观察行进，
缩放，得到的是速度值，所以比例和前面得到角度的比例不一样。

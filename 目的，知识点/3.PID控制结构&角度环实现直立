1.
pid控制结构

a,pid
pid是一个闭环控制算法。
动态改变，作用于被控制对象的，来源于控制器的输出值，实现被控对象实际值准确跟踪目标值的功能。

控制器，获取目标值与内控对象返回的实际值，来动态改变输出值，

PID输出值:  out(t) = Kp * error(t) +Ki *(error(t)dt)的积分 + Kd*derror(t)/dt
pid输出值，有三项组成，比例项，积分项，微分项。

设计思路：
平衡车的前进，是两个轮子同时前进
回退，是两个轮子同时后腿
左转，左轮速度大于右轮
右转，左轮速度小于右轮。
可以看出，底层原理出发，进行程序设计，实现的功能，需要分别控制两个轮子的速度，真不方便用户体验。
我们，需要将底层程序解耦，程序设计上，不对两个轮子的pwm操作，而是：

定义2个变量，分别实现前进后退/左右转弯。

平均pwm，差分pwm。分别控制前后，左右运动的快慢。
底层控制电机运动，需要计算左右轮的pwm，返回给左右电机。
定义：
输出信号量转换
左轮pwm=平均pwm+差分pwm/2.
右轮pwm=平均pwm -差分pwm /2
输入信号量转换：
平均速度=(左轮速度+右轮速度)/2
差分速度=左轮速度-右轮速度


所以，
控制器的输入量是：实际值：平均速度，差分速度
目标值：
输出量是：平均pwm，差分pwm

pid控制器，三环（控制环）理解：
a,角度环
输入量：
目标角度，手动指定，或者外环动态输出
实际角度，被控对象返回，mpu6050计算得到。

输出量：平均pwm。小车，倾斜角为0，稳稳直立，不动。输出平均pwm，来实现这一效果
倾斜角>0,需要前进，来保持倾斜角不变，所以，需要输出平均 pwm来以精确速度前进
倾斜角<0,需要后腿，来保持倾斜角不变，所以，需要输出平均 pwm来以精确速度后退

输入量的目标角度，不能手动给定。
因为，小车倾斜，会产生水平方向的加速度，如果不调节，运动速度会越来越快，
最终输出平均pwm大于电机极限速度对应的pwm，
电机跟不上，导致倒地。
如果在这个过程，调节加速度，来抵消这个影响，就可以了。也就是角度需要不断调节
所以，输入量的目标角度，不能手动给定，采用外环的输出值。

引出外环的设计：
外环是速度环，内环是角度环。

为了，解决倾斜带来的加速度的，对速度的影响，我们需要动态调节内环角度，外环速度。
输出量：内环的目标角度
输入量：
实际速度，平均速度，平衡车返回得到。
目标速度，手动输入。
当，目标速度<实际速度,
说明倾斜角带来的加速度影响已经生效，我们需要减小倾斜角。
外环输出小的倾斜角，平衡车输出大的倾斜角
内环，得到外环输出的新的目标角度与平衡车的新的倾斜角作为输入目标值，来调节输出。
此时，目标值：倾斜角<实际值：平衡车倾斜角。
内环增大平均pwm的输出，通过加速，来减小倾斜角。
这是双环联动的原理。

总之：用户操作时，只需要改变速度环的输入值:目标速度,
就可以实现目标角度的改变，进而实现平均pwm的输出。（而且，倾斜角的影响被解决了）


转向环：
由于，两个电机性能存在微小差异，所以如果只是给一个差分pwm，还是不能够按照预期效果运行。
需要，借助转向环来动态调节转向效果。
输入量：差分速度。左右转弯量
输出量：差分pwm

总之，通过左右转弯量，可以控制小车的左右转弯

上面的三环设计，是一种方案。还有其他方案可以探索。



2.
角度环代码功能实现

输出值,由pid算法得到。
pid的底层文件，结构体有多个变量。
我先选取，kp,ki,kd，outmax,outmin.5个变量。

pid算法需要确定调控周期，
角度环对应的pid调控周期，我选取为10ms。

前面，计算角度的周期也需要跟着改为10ms。
互补滤波算法的参数a,也需要跟着改变。
原先，1ms执行一次滤波算法，a是0.001，
现在是，10ms执行一次滤波算法，a改为0.01.
这样，每一个周期内的作用效果不变，向加速度测量得到的角度进行偏移的效果不变。

结构体类型定义后，进行结构体变量定义。（）结构体成员与定义一样）
语法格式如下：（选取5个变量定义，其他变量在后面赋值定义）
PID_t pangle_PID=
{
.Kp=0,
.ki=0,
.kp=0,
.outmax=100,
.outmin=-100,
};

平均pwm的输出最大，最小值，与左右轮的pwm范围一样。
由自己规定，小于电机最大转速即可。我规定100

角度环调控(输出平均pwm )三步走：
得到角度值
pid算法实现
将pid输出值作为平均pwm,

后面，再进行左右电机pwm的计算。
并传入电机运行函数

然后：


3.
调节pid参数，kp,ki,kd 

如果，改一次，下载一次比较麻烦。
通过写判断逻辑，
我使用蓝牙的摇杆功能，移动摇杆，改变摇杆对应的参数值，来远程调参。

并在oled上显示，kp,ki,kd,目标值，实际值，输出值。来观察调节效果

占位符含义：
%05.2f
%+05.1f
%+05.0f

调节极性：
在前倾时，轮子应该向前转。
当轮子向后转，说明我们的输出值，平均pwm，极性反了。
需要手动取反


当角度误差小时，kp项作用很弱，所以需要加上积分项，ki。
但是不能给大，积分作用会很强，导致超调过多

加上微分项，kd，来抑制超调与振荡

当然，pid调参主要还是，硬件合不合理，软件程序有没有正常工作，如果合理，参数很好调


如果，小车有抖动情况，可能是mpu6050并不是完全水平放置。
可以读取加速度测量的角度，并显示来查看。
为了抵消这个误差，加上某个数，使其为0即可
然后，后面加上速度环调控，就觉得不会出现这个问题

继续优化程序，
pid底层计算输出值的接口，内部进行pid计算。
里面的参数，如：误差值，不会丢失，下一次按钮按下，来不及mpu6050计算之前，
会先立即执行上一次的参数，得到输出值
当小车倒地的过程积累很多误差，下一次按下启动按钮（不是初始化）
会导致小车立即发疯，转速很大
所以，在启动的标志位设置前，需要将pid的参数清零。  

4.
实现角度环控制前进与转弯：

滑杆来远程调节目标角度，差分pwm。
左摇杆纵向值，调节角度，进而调节加速度实现前进，（但是效果肯定不如速度环作为外环）
为了精确调节，把摇杆值进行缩放，再赋值给目标角度，差分pwm 。直行效果也不如转弯换的控制
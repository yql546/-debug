1.
实现系统功能，需要逐个添加逻辑
逻辑的实现，从底层准备到程序逻辑

按键控制启停

按键反转标志位，实现led控制。



2.

角度环pid控制。
按键启停：角度环pid控制放在按键启停的标志位判断里，这个要放在判断是否放倒的逻辑后面

角度环调控(输出平均pwm )三步走：
得到角度值
pid算法实现
将pid输出值作为平均pwm,

再进行左右电机pwm的计算
得到左右电机pwm，需要先进行上限处理。上限为100，-100
再传给电机运行函数

pid算法实现，需要调用底层的封装接口

执行完pid调控得到输出值：平均pwm，与电机运行参数传入后，（差分pwm默认为0）

开始调节pid的参数。

移植蓝牙模块的主程序。
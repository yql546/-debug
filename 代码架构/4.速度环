
1.
在中断里，进行分频。
定时服务函数里，
第一步获取encoder的编码值，
参数定义
进行信号量的转换，得到平均速度
第二步，
处于按钮按下，的开启状态，进行速度环pid的配置
参数定义：结构体配置
输出值最大值为20，-20.角度为20我们就认为调节效果不好了
获取输入的实际值，
pid接口返回out输出值，
将out作为内环角度环的输入
每次按下启动按钮，将速度环的参数初始化，


然后，调pid参数
用蓝牙滑杆调参。

一，目的：
移植平衡车需要的硬件驱动，写简单的测试程序
不同模块隔离测试，
如果简单的测试程序都不能实现，说明是硬件问题。
注意点：
查看原理图，修改驱动的引脚配置。


分配定时器，配置定时器端口。
定时器分配：
timer1用于定时中断
timer2用于驱动两个电机转动
timer3，4分别测试两个电机的速度与角度

二，知识点： 
1，定时器
time1是高级定时器，在apb2总线上。

高级定时器的函数，参数名称与timer2,3,4不同。
参数名称查看定义来填写。
中断函数名称到驱动文件查看，startup_stm32f10x_md.s
调用定时中断，就必须调用定时中断函数，否则，若定时时间到，会卡死在定时中断里。

定时的频率，与两个参数有关，
TIM_Period = 10000 - 1;
TIM_Prescaler = 7200 - 1;
频率=72000000/10000/7200=   1Hz
定时时间=1/frequency=1s

2，按键

移植非阻塞式按键。
功能：
实现非阻塞式按键，还需要在定时中断里调用，key_tick函数，来检测按键状态。
按键状态分为：按下，松开，长按，连续按。

查看，原理图，修改底层驱动。
1

3.mpu6050
读取x，y,z三轴速度，加速度

由于，需要定时准确读取数据，所以将获取数据的函数放到定时中断里。
但是，由于这个函数需要的运行时间较长，可能对于定时时间，所以需要进行处理，以缩短时间。

下面，介绍两个伪代码的不同结果，来查看函数的运行时间，并来理解定时中断
（一个原则：中断函数期间不会被打断）
将清除标志位与获取数据的函数，分别调位，
a
清除标志位
获取数据
标志位清0，获取数据的函数为运行完，定时时间到，标志位设置为1，等待本中断函数结束，立即进入下一个中断，
这会导致，没有给主程序留下运行时间，所以oled不会显示
b
获取数据
清除标志位
获取数据的函数为运行完，
才清标志位，等待下一个定时时间到，设置标志位为1，才会进入新的中断函数
如果，刷新时间比在while(1)里慢，说明函数执行时间大于定时时间。

下面介绍另一种判断中断是否重叠的方法
c
 清除标志位
获取数据   
嵌套一个中断函数，oled打印数据来判断是否重叠。
注意，由于oled打印函数执行时间忽略不计，所以中断函数立即结束后，进入主程序。


下面介绍，如何处理获取数据的函数，来减少运行时间。
注释掉延时函数，对于72mh主频的单片机，i2c还是能够跟的上的。

发现，oled的打印结果说明不会发生重叠，但是，刷新速度还是小于在while(1)里，说明中断函数占用的时间还是太多了，与上面b同理。

（下面写一个逻辑功能，来计算中断占用时间。
在中断函数末尾，调用函数，看一下定时器计算到哪里 了
TimerCount = TIM_GetCounter(TIMl);打印TimerCount显示。）

下面进一步优化获取数据函数的内部算法。
将启动一次i2c时序，读取一个字节的逻辑改为读取多个字节。
封装的函数，需要采用for循环在内部多次读取数据，
所以函数需要参入数据指针参数，与数据长度参数。




